// import { getByUserId, getByUserProjectId, addItemToCalendar } from '../index'

describe('API Controller: calendar', () => {
  describe('GET full user calendar (multi project) between 2 dates', () => {
    it('should respond with 200', async () => {
      // const ctx = { params: { id: '11-22-33' }, status: undefined }
      // await getByUserId(ctx)
      // expect(ctx.status).toEqual(200)
    })
  })

  describe('GET user calendar of 1 project between 2 dates', () => {
    it('should respond with 200', async () => {
      // const ctx = { params: { id: '11-22-33' }, status: undefined }
      // await getByUserProjectId(ctx)
      // expect(ctx.status).toEqual(200)
    })
  })

  describe('POST an item to user calendar for 1 project', () => {
    it('should respond with 200', async () => {
      // const ctx = { params: { id: '11-22-33' }, status: undefined }
      // await addItemToCalendar(ctx)
      // expect(ctx.status).toEqual(200)
    })
  })
})
