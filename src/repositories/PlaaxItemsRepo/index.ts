export const getFullUserBoard = (userId: string): undefined | object => {
  // get all user projects, loop and call getUserProjectBoard
  return {}
}

export const getUserProjectBoard = (userId: string, projectId: string): undefined | object => {
  return {}
}
